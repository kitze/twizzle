const SIZES = {
  SMALL: 'small',
  LARGE: 'large'
};

const IPC = {
  OPEN_SETTINGS: 'open-settings',
  REFRESH_WEBVIEW: 'refresh-webview',
  QUIT: 'quit',
  RESIZE: 'resize'
};

const STORE = {
  SHOW_TWEET_COMPOSER: 'showTweetComposer',
  SIZE: 'size',
  NIGHT_MODE: 'nightMode',
  SHOW_SETTINGS: 'showSettings',
  IS_AUTHED: 'isAuthed',
  URL: 'url'
};

module.exports = {
  SIZES,
  IPC,
  STORE
};
